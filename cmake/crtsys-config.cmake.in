@PACKAGE_INIT@

set(crtsys_ROOT "${PACKAGE_PREFIX_DIR}")
set(CRTSYS_ROOT "${PACKAGE_PREFIX_DIR}")
if(NOT DEFINED CRTSYS_ALLOW_NETWORK_DEPENDENCIES)
  set(CRTSYS_ALLOW_NETWORK_DEPENDENCIES OFF)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/CrtSys.cmake")

check_required_components(crtsys)
